import styles from '../styles/Home.module.css'

const Footer = () => {
    return (
        <div className={styles.footer}>
            <p>footer from the NEXT application</p>
        </div>
    )
}

export default Footer